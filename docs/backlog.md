# Бэклог (перенос из рабочей сессии 2026-09-12)

Действующие задачи по убыванию приоритета. Исполнители: Ф (фронтенд-чат),
Б (бэкенд-чат), QA, Д (деплоер). Пункты «согласовать» — аналитик обсуждает
с владельцем перед выдачей промпта.

## Задачи

### 1. ~~UI для merge-инструмента~~ — ✅ сделано (2f87629, в dev)
API готов (`POST /admin/students/merge`: без `confirm` возвращает превью
`records_to_merge`, с `confirm` выполняет; `rewrite_names` — перезапись
отображаемых ФИО). Нужна страница/секция в админке: два поля ФИО,
кнопка «Предпросмотр» → показать счётчик → чекбокс «переписать ФИО в
записях» → «Объединить» с подтверждением. Form-encoded, CSRF как обычно.

### 2. ~~Подсказка существующих ФИО при заполнении профиля~~ — ✅ сделано (8b86910 + 7420d3e, в dev; приватность — см. data-model-decisions.md)
Привязка записей посимвольная: «Иванов И. И.» ≠ «Иванов Иван Иванович»,
опечатки ломают привязку чаще, чем тёзки. Бэкенд: `GET /api/students` —
уникальные ФИО из записей. Фронтенд: datalist в форме профиля и в
админской привязке псевдонима. Статус: согласовать с владельцем.

### 3. ~~Аудит-журнал безопасности~~ — ✅ сделано (бэкенд + минимальная страница /admin/audit, в dev; решение — data-model-decisions.md)
Таблица + записи: входы (успешные/нет), смена паролей, действия модерации
(подтвердить/отклонить с комментарием), merge. Просмотр — страница админа.
Обсуждалось как недостающие 20% «спокойной» аутентификации.

### 4. Пагинация таблицы — Б + Ф, «на триггере»
Триггер: записей заметно больше ~500–1000 или массовый приход атлетов.
Серверная постраничная выдача, фильтры/сортировка на сервере, экспорт
по-прежнему выгружает всё.

### 5. 2FA (TOTP) для админов — Б + Ф, опционально
Обсуждалось как желательное для «городского» сценария; для доверенного
круга не нужно.

## Мелочи (брать пачкой, когда попутно)

- Rate-limiter логина живёт в памяти процесса — при переходе на несколько
  воркеров вынести в общий store.
- В xlsx-экспорте строки, начинающиеся с `-` (например, телефоны),
  получают защитный апостроф — косметика, осознанный компромисс.
- Dockerfile: контейнер работает от root; добавить USER — low.
- Перед публикацией репозитория: вычистить email авторов из метаданных
  коммитов (git filter-repo) — вместе с повторной проверкой истории.

## Ад-хок сессии (создаются по случаю, не постоянно)

- Аудитор безопасности — разовый полный аудит перед любым «показом вузу».
- Техписатель — когда дойдёт до quickstart из future-ideas.md.

## Решено без кода (не делать)

- Заметность импорта — закрыта новой навигацией (импорт живёт на /admin).
- Тёзки (одинаковые ФИО у разных людей) — не обрабатываем заранее:
  просмотр чужих записей тезкой — редкость, порча данных исключена
  модерацией. Точечное решение при реальном случае (см.
  data-model-decisions.md).

## Новые пожелания владельца (2026-09-12, вторая волна) — на обсуждение/планирование

8. Выгрузка базы в Excel и очистка базы — вынести в админку; очистка
   защищена от случайного нажатия (двойное подтверждение).
9. UX добавления записей: убрать громоздкие блоки, «Добавить строку»
   с автоподстановкой основных полей из профиля, остальные дозаполнить
   обязательно (кроме вложений). Аналогично пересмотреть блок в админке.)
10. Экспорт отчёта в Excel + конфигурация отчёта (админ выбирает поля).
11. Админка: разграничить по блокам/страницам (сейчас всё в кучу).
12. Справочники (уровни, виды спорта и т.п.) — управление админом,
    не хардкод.
13. Импорт: вынести из админки на страницу таблицы (или два режима —
    подумать).
14. Тёмная тема.

Плюс мелочь от QA: обернуть get_audit_events на /admin/audit в try/except
(500 на физически удалённой таблице — не блокер).


## Инцидент CSRF (2026-09-12) — закрыт

Удаление/approve/reject/вложение/clean_db в браузере получали 403: makeRequest
прикреплял csrf-токен только к FormData. Починено универсально (42a283b),
проверено реальным браузером; на прод — горячая замена script.js деплоером
(docker cp, временно до пересборки образа). Урок: curl-тесты с вручную
вычисленным токеном этот класс не ловят — в QA-матрицу добавлен браузерный
блок.


## QA вторая волна (2026-09-12, dev 0b4b7ec) — принято

Все 10 блоков зелёные, 144 pytest, блокеров нет. Браузерный блок
(клики удаления/approve/reject/вложений, две вкладки) — пройден.
Вердикт: готово к деплою после docs-задачи (ридми/роли/квикстарт —
кода не меняют, перепригона не требуется).

Наблюдения QA (косметика, в мелочи):
- POST на несуществующий роут даёт 403 CSRF вместо 404: middleware
  проверяет CSRF до матчинга роута, тело unmatched-роута не парсится.
  Безопасности не угрожает, сообщение вводит в заблуждение при отладке.
- Селект на /reports в тёмной теме — серый фон вместо тёмного, как у
  остальных контролов (контраст 5.3:1, AA проходит). nice-select2.

## Перенос утверждённого дизайна в продукт (одобрено владельцем 2026-09-13)

Дизайн-прототип v0.2 утверждён (design/prototype/). Перенос в продукт:
токены → CSS поверх Bootstrap, постранично (таблица → админка → отчёты),
с сохранением обеих тем. Запуск — по слову владельца, после текущего
пакета (отчёты → QA → выкат) отдельной волной.

## Пакет фидбека 2026-09-13 (№1-19 из design/prototype/FEEDBACK.md) — код завершён

Всё в dev (381c4c4): UX таблицы (инлайн-ввод, гибридные даты, sticky
действий, dblclick), обслуживание БД (панель состояния+диск, ручной
бэкап, очистка вложения/записи/всё + по дате соревнования с превью
размера, pre-wipe архивы xlsx+zip со скачиванием, VACUUM, аудит бэкапов),
журнал аудита (фильтры, пагинация), иерархия институт→группа с
комбобоксами, присутствие пользователей (онлайн/был(а)), отчёты
(7 срезов, метрики побед/призовых, фильтры справочников, экспорт
наследует всё). 210 тестов. Далее: сводный QA → выкат.

## QA-сводный прогон пакета (2026-09-13, dev effdf26) — принято с одной находкой

Все 9 блоков зелёные, 210 pytest. НАХОДКА №1 (в работе до выката):
импорт xlsx выполняется в event loop с построчным коммитом+fsync —
0,22 с/строку и полная заморозка приложения (100 строк = 22 с,
500 = 1,5 мин, /healthcheck не отвечает). Лечение: батч-транзакция +
asyncio.to_thread. Наблюдение-косметика (перенос): серый селект на
/reports в тёмной теме — в мелочи.

Перепроверка импорта (dev 5d6af00): фикс подтверждён QA — 1000 строк
за 0,30 с без блокировки приложения, атомарность и справочники в
порядке, 213 тестов. Блокеров к релизу нет → пакет передан на выкат.

## Выкат 831a814 (2026-09-13) — wave0+wave1 на проде

nan-фикс, регистровые справочники с канонизацией, аккордеон институтов,
автозаполнение института по группе, кликабельные url-поля (gate http/https).
Открытый хвост: чистка уже созданных «nan»-строк на проде (решение после
подсчёта); короткий QA-контроль выката.

## QA волн 2-3.5 (2026-09-13, dev 8f48b90) — принято, багов нет

Все 5 блоков зелёные, 321 pytest. Даты (4 формата, пересечение фильтра,
roundtrip), реестр полей (text-Курс, audit), очередь конфликтов
(принять/пропустить + аудит), резолвер ФИО (браузер, приватность athlete),
регресс. Наблюдения: отчёт агрегированный — дат там и не должно быть;
/export/index не наследует фильтры главной (прежнее поведение, кандидат
в мелкие улучшения). В тесты добавить 5 кейсов пересечения период-фильтра.

## Выкат 6e1105d + QA (2026-09-13) — пакет волн 0-3.5 закрыт

Хотфикс инлайн-создания, очередь в вертикальном UX (пропустить/заменить/
решить вручную), тумблер подсказок — на проде. QA чист (334 теста,
5 блоков). Наблюдение: при скрытой колонке «Название» инлайн-создание
даёт пустое название (логично; можно подсветить в подсказке).
Дальше: календарь (№23) — макет 15/16 утверждён, реализация в 2 волны.

## Календарь соревнований (№23) — код завершён (2026-09-13, dev e4d25f5)

Wave A (сущность + /calendar, 6f96be7) + Wave B (участники с резолвером
ФИО, «результаты потом», e4d25f5). 360 тестов. Далее: QA календаря →
выкат → smoke дяди. Обсуждение «nan»-чистки и мелочи — в хвостах.
